[package]
name = "lerpz-utils"
edition = "2024"
version.workspace = true

[dependencies]
chrono = { workspace = true, optional = true }
thiserror = { workspace = true }

[features]
upn = ["dep:chrono"]
